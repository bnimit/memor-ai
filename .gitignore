__pycache__/
*.pyc
.venv/
*.db
.memorable/
dist/
build/
*.egg-info/
docs/superpowers/

# session scratch + superpowers artifacts (not for release)
scratch_*.py
.superpowers/
/scratch/

# uv resolves its own lock from pyproject.toml; the project has never
# committed one, and a lock generated on one machine's Python would pin
# resolutions the published package does not intend.
uv.lock

.DS_Store
